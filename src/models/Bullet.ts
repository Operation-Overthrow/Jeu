export class Bullet {
    public velocity: number;
    public x: number;
    public y: number;

    constructor(x:number,y:number,velocity:number){
        this.velocity = velocity;
        this.x = x;
        this.y = y;
    }

}